import os
import pathlib
import platform
from distutils.core import setup

from setuptools import setup, find_packages

setup(
    name="turtlebot3_ppo",
    version="0.1.0",
    packages=find_packages(),
)
